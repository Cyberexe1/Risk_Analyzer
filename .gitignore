__pycache__/
*.py[cod]
.venv/
venv/

# Generated data. Reproducible with:
#   python ml/generate_dataset.py --n 100000
# Deterministic given the fixed seed, so there is no reason to version a 20 MB CSV.
ml/data/*.csv

# Trained artifacts are kept in git on purpose: metrics.json is the source of
# truth the README is checked against, and model.json is small.
!ml/artifacts/

.env
.DS_Store

# scratch
_demo_out.txt

# build
build/
*.egg-info/
*.log
.pytest_cache/

# frontend
web/node_modules/
web/dist/
web/.env
